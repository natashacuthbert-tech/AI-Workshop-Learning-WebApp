# TODO

## Speaker cards redesign (premium compact grid)

- [ ] Inspect existing speaker card markup + CSS classes in `frontend/src/components/SpeakerCard.jsx` and `frontend/src/App.css`.
- [ ] Create new card component markup/classes from scratch (no rearranging old layout).
- [ ] Add new responsive CSS Grid (3/2/1 columns) and fixed-ish card dimensions (380px width, 480–520px height; equal height).
- [ ] Implement new “compact Apple Events / Google I/O style” glassmorphism card styling + hover animation.
- [ ] Ensure card structure matches spec: centered profile image on top, name/org/title, category badge, session title in glass container, one-row tags, centered “View Profile” button.
- [ ] Remove/override old speaker card styles that conflict with the new design.
- [ ] Run frontend build/lint/test command(s) if available.

